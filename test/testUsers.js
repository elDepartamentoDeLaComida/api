var Lab = require("lab"),
  serializeJSON = require("../utils/myUtils").serializeJSON,
  server = require('../index').server;
/*
Lab.experiment("Users:" funtion () {

  Lab.test("User can be registered")
});
*/